[package]
name = "endif-sim"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Deterministic TF2-accurate simulation for endif (soldier 1v1 airshot minigame)"

[dependencies]
serde = { workspace = true }
bytemuck = { workspace = true }
libm = { workspace = true }

[dev-dependencies]
# `tests/netsweep.rs`: two GGRS peers over a simulated link.
ggrs = "0.13"
